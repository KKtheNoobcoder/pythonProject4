temperature = int(input("temperature: "))
unit = input("(F) or (C): ")
if unit.upper() == "C":
    convert = (temperature * 1.8) + 32
    print("temperature in °F: " + str(convert))
else:
    convert = (temperature - 32) / 1.8
    print("temperature in °C: " + str(convert))